MATHMODULE.JS

// mathModule.js

// Function to add two numbers
export function add(a, b) {
    return a + b;
}

// Function to multiply two numbers
function multiply(a, b) {
    return a * b;
}

// Default export for the multiply function
export default multiply;
